{
  "name": "pistol-search",
  "private": true,
  "version": "0.1.0",
  "description": "Online tool for finding handguns matching particular specifications.",
  "repository": "https://github.com/rabdill/pistolsearch.git",
  "license": "GPL-3.0",
  "devDependencies": {
    "http-server": "^0.6.1",
    "jasmine": "^2.4.1",
    "protractor": "^3.2.2"
  },
  "scripts": {
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1",
    "test": "protractor .protractor-config.js"
  }
}
